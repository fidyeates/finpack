from setuptools import setup, find_packages
setup(
    name="finpack",
    version="0.1",
    package_dir={'': 'src'},
    packages=['finpack'],
    scripts=[],

    install_requires=[],
    package_data={},

    # metadata for upload to PyPI
    author="",
    author_email="",
    description="",
    license="PSF",
    keywords="finpack",
    url="",  # project home page, if any
)
